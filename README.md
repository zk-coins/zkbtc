# zkbtc
zkBTC — a 1:1 BTC-backed, freely transferable, redeemable token on the zkCoins protocol (token standard 3): works-today BitVM2 reserve, open permissionless operator registration, optional mint gatekeeper.
